global:
  scrape_interval: 10s
  evaluation_interval: 30s

scrape_configs:
  - job_name: "ocserv"
    # Exporter 默认暴露 /metrics 路径
    metrics_path: /metrics
    static_configs:
      - targets: ["ocserv:9100"]
    scrape_timeout: 3s
